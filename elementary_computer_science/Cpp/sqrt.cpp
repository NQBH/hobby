#include <bits/stdc++.h>
#include <cmath>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	long double x = sqrt(2);
	long double y = sqrt(3);
	printf("%.60Lf, %.60Lf\n", x, y);
}